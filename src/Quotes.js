import React from 'react';
import './data.js'

class Quotes extends React.Component {
    render () {
        return (
            {returnedQuote.quote}
        )
    }
}


export default Quotes
// const getLizzoQuote = lizzoQuotes[Math.floor(Math.random) * lizzoQuotes.length]
// console.log(getLizzoQuote())